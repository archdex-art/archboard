{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Archboard's main window. Process launching lives behind typed Rust commands, so no shell capability is granted here.",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default",
    "opener:allow-open-url",
    "opener:allow-reveal-item-in-dir",
    "dialog:allow-open",
    "window-state:default"
  ]
}
